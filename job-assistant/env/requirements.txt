# Python dependencies for the local job-application-assistant tool.
# Install inside a Python 3.11+ virtual environment (see ../env/setup.md).
#
# sqlite3 is part of the Python standard library and does not need to be
# listed here.

requests>=2.31.0
playwright>=1.44.0
python-dotenv>=1.0.1
